import { Section } from '@/components/common/Section';
import { Text } from 'react-native';

export default function AccountScreen() {
  return (
    <Section title="Tu cuenta" subtitle="Editar datos de tu cuenta">
      <Text>Account</Text>
    </Section>
  );
}
